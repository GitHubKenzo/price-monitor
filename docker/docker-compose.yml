version: "3.8"

services:
  scraper:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: price-monitor-scraper
    working_dir: /workspace
    environment:
      - TZ=Asia/Tokyo
