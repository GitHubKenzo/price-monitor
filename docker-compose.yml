version: "3.8"

services:
  scraper:
    build: .
    container_name: price-monitor-scraper
    env_file:
      - .env
    volumes:
      - ./scraper:/app/scraper
      - ./data:/app/data
    environment:
      - TZ=Asia/Tokyo
