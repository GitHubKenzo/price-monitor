version: "3.8"

services:
  scraper:
    build: .
    container_name: price-monitor-scraper
    volumes:
      - ./scraper:/app/scraper
      - ./data:/app/data
    environment:
      - TZ=Asia/Tokyo
      # LINE_NOTIFY_TOKEN=xxxxx を使う場合はここに追加
      # - LINE_NOTIFY_TOKEN=YOUR_TOKEN
